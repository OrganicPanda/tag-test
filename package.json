{
  "name": "tags",
  "version": "1.0.11",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preversion": "node -p \"console.log('from', require('./package.json').version, 'to', process.env.npm_package_version)\"",
    "print": "node -p \"require('./package.json').version\""
  },
  "author": "",
  "license": "ISC"
}
