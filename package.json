{
  "name": "tags",
  "version": "1.8.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preversion": "./bin/preversion",
    "postversion": "git push upstream && git push upstream v$npm_package_version",
    "print": "node -p \"require('./package.json').version\""
  },
  "author": "",
  "license": "ISC",
  "repository": "OrganicPanda/tag-test"
}
